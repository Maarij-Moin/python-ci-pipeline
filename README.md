# Python CI Pipeline (Practice)

## Overview

A minimal Python project created to practice automated testing and CI pipelines using GitHub Actions.

## What it covers

* Writing unit tests with pytest
* Running tests locally
* Setting up a basic CI pipeline

## How to run tests

```bash
python -m pytest
```

## CI

Tests run automatically on every push using GitHub Actions.

---
